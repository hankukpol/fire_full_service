# 경찰 → 소방 변환 핵심 변경사항

> **AI 협업자 참고**: 이 문서는 `police/` 프로젝트에서 `fire/`로 변환하면서 실제로 수정된
> 파일과 변경 내용을 상세히 기술합니다. 코드 수정 시 반드시 확인하세요.

---

## 1. ExamType 변환

| 경찰 (`police/`) | 소방 (`fire/`) |
|----------------|--------------|
| `PUBLIC` (공채, 통합선발) | `PUBLIC` (공채, 남/여 **분리**선발) |
| `CAREER` (경행경채) | `CAREER_RESCUE` (구조, 남자만) |
| *(없음)* | `CAREER_ACADEMIC` (소방학과, 지역별 남/여/양성) |
| *(없음)* | `CAREER_EMT` (구급, 남/여 분리선발) |

> 경찰의 2종 → 소방의 4종으로 확장됨

---

## 2. ExamRegionQuota 필드명 변경

경찰과 소방은 DB 필드명이 완전히 다릅니다.

| 경찰 (OLD) | 소방 (NEW) | 비고 |
|-----------|-----------|------|
| `recruitCountPublicMale` | `recruitPublicMale` | |
| `recruitCountPublicFemale` | `recruitPublicFemale` | |
| `recruitCountCareerRescue` | `recruitRescue` | |
| `recruitCountCareerEmt` | `recruitEmtMale` / `recruitEmtFemale` | 남녀 분리됨 |
| *(없음)* | `recruitAcademicMale` | 소방학과 신규 |
| *(없음)* | `recruitAcademicFemale` | 소방학과 신규 |
| *(없음)* | `recruitAcademicCombined` | 소방학과 양성 신규 |
| `applicantCountPublicMale` | `applicantPublicMale` | |
| `applicantCountPublicFemale` | `applicantPublicFemale` | |
| `applicantCountCareerRescue` | `applicantRescue` | |
| `applicantCountCareerEmt` | `applicantEmtMale` / `applicantEmtFemale` | 남녀 분리됨 |

---

## 3. 채점 엔진 변경 (`src/lib/scoring.ts`)

### 3-1. SUBJECT_RULES 변경

```typescript
// 경찰
[ExamType.CAREER]: [
  { name: "범죄학 or 헌법", questionCount: 20, pointPerQuestion: 2.5, maxScore: 50 },
  { name: "형사법", questionCount: 40, pointPerQuestion: 2.5, maxScore: 100 },
  { name: "경찰학", questionCount: 40, pointPerQuestion: 2.5, maxScore: 100 },
],

// 소방 (4종)
[ExamType.PUBLIC]: [3과목 75문항 300점],
[ExamType.CAREER_RESCUE]: [2과목 65문항 200점 — 소방학개론+소방관계법규],
[ExamType.CAREER_ACADEMIC]: [2과목 65문항 200점 — 소방학개론+소방관계법규],  // 구조와 동일
[ExamType.CAREER_EMT]: [2과목 65문항 200점 — 소방학개론+응급처치학개론],
```

### 3-2. 총점 과락 추가 (소방 전용)

```typescript
// 경찰에는 없음, 소방에만 있음
const totalMaxScore = subjects.reduce((sum, s) => sum + s.maxScore, 0);
const hasTotalCutoff = totalScore < totalMaxScore * TOTAL_CUTOFF_RATE; // 60%
```

### 3-3. toRankGroupKey 변경

```typescript
// 경찰: 성별 구분 없음 (통합선발)
// 소방: CAREER_RESCUE 제외 모두 성별 포함
function toRankGroupKey(regionId, examType, gender) {
  if (examType === ExamType.CAREER_RESCUE) return `${regionId}:${examType}`;
  return gender ? `${regionId}:${examType}:${gender}` : `${regionId}:${examType}`;
}
```

---

## 4. 예측 엔진 변경 (`src/lib/prediction.ts`)

### 4-1. toExamTypeLabel 변경

```typescript
// 경찰
PUBLIC → "공채"
CAREER → "경행경채"

// 소방
PUBLIC → "공채(남)" / "공채(여)" / "공채" (성별에 따라)
CAREER_RESCUE → "구조 경채"
CAREER_ACADEMIC → "소방학과(남)" / "소방학과(여)" / "소방학과"
CAREER_EMT → "구급(남)" / "구급(여)" / "구급 경채"
```

### 4-2. getRecruitCount 변경 (지역 quota 기반)

```typescript
// 경찰: 단순 조회
// 소방: CAREER_ACADEMIC은 양성 여부 확인 후 분기
case ExamType.CAREER_ACADEMIC:
  if (quota.recruitAcademicCombined > 0) return quota.recruitAcademicCombined;
  return gender === MALE ? quota.recruitAcademicMale : quota.recruitAcademicFemale;
```

### 4-3. 합격배수 테이블 분리

```typescript
// 경찰: 단일 테이블 (1.5~1.8배)
// 소방: 공채/경채 별도 테이블
getPublicPassMultiple(recruitCount)  // 1.5~3배
getCareerPassMultiple(recruitCount)  // 1.5~1.8배 + 5명 이하 특수
```

### 4-4. buildPopulationWhere 변경

```typescript
// 경찰: 단순 { examId, regionId, examType }
// 소방: CAREER_ACADEMIC은 quota 확인 후 성별 필터 결정
function buildPopulationWhere(submission, quota?) {
  if (examType === CAREER_ACADEMIC && quota?.recruitAcademicCombined > 0) {
    // 양성 지역: 성별 무관 전체 경쟁
    return { examId, regionId, examType };
  }
  // 나머지: 성별 포함
  return { examId, regionId, examType, gender };
}
```

---

## 5. 수정된 파일 전체 목록 (경찰 대비)

### 핵심 엔진 (완전 재작성)
| 파일 | 변경 내용 |
|------|---------|
| `prisma/schema.prisma` | ExamType 4종, ExamRegionQuota 8필드, certificateBonus |
| `prisma/seed.ts` | 소방 18개 지역 + 4종 모집인원 + 소방 과목 |
| `src/lib/scoring.ts` | 4종 SUBJECT_RULES, 총점 과락, 성별 기반 rank key |
| `src/lib/prediction.ts` | 4종 label, 합격배수 2테이블, quota 기반 population |
| `src/lib/policy.ts` | TOTAL_CUTOFF_RATE=0.6, 자격증 옵션 |
| `src/types/index.ts` | RegionRecruitInfo 8필드 |

### API 라우트 (ExamType 4종 + 필드명 반영)
| 파일 | 주요 변경 |
|------|---------|
| `api/admin/regions/route.ts` | 8필드 CRUD |
| `api/admin/submissions/route.ts` | CAREER_ACADEMIC 추가 |
| `api/admin/rescore/route.ts` | CAREER_ACADEMIC 추가 |
| `api/admin/answers/route.ts` | CAREER_ACADEMIC 추가 |
| `api/admin/answers/preview/route.ts` | CAREER_ACADEMIC 추가 |
| `api/admin/answers/logs/route.ts` | CAREER_ACADEMIC 추가 |
| `api/main-stats/route.ts` | 8필드 + CAREER_ACADEMIC |
| `api/submission/route.ts` | CAREER_ACADEMIC, certificateBonus |
| `api/stats/route.ts` | CAREER_ACADEMIC 추가 |
| `api/exams/route.ts` | 8필드 + CAREER_ACADEMIC |
| `api/result/route.ts` | CAREER_ACADEMIC 과목 순서 |
| `api/analysis/answer-change-impact/route.ts` | CAREER_ACADEMIC |
| `api/analysis/subject-stats/route.ts` | CAREER_ACADEMIC 과목 |
| `api/pass-cut-history/route.ts` | CAREER_ACADEMIC |
| `api/final-prediction/route.ts` | CAREER_ACADEMIC 레이블 |
| `api/exam-number/check/route.ts` | CAREER_ACADEMIC |
| `api/share/data/route.ts` | CAREER_ACADEMIC 레이블 |

### 프론트엔드 페이지 (ExamType 4종 반영)
| 파일 | 주요 변경 |
|------|---------|
| `app/exam/input/page.tsx` | 4종 채용유형, 동적 OMR (75/65문항), 자격증 가산점 |
| `app/exam/result/page.tsx` | CAREER_ACADEMIC 레이블 |
| `app/exam/prediction/page.tsx` | CAREER_ACADEMIC 레이블 |
| `app/exam/result/types.ts` | CAREER_ACADEMIC 타입 |
| `app/admin/page.tsx` | 5열 → 6열 지역현황 (구조/소방학과 분리) |
| `app/admin/regions/page.tsx` | 8필드 모집인원 UI |
| `app/admin/answers/page.tsx` | CAREER_ACADEMIC 탭 |
| `app/admin/stats/page.tsx` | CAREER_ACADEMIC 통계 |
| `app/admin/submissions/page.tsx` | CAREER_ACADEMIC 필터 |
| `app/page.tsx` | CAREER_ACADEMIC 참여자 수 |

### 컴포넌트
| 파일 | 주요 변경 |
|------|---------|
| `components/landing/ExamMainOverviewPanel.tsx` | CAREER_ACADEMIC |
| `components/landing/DifficultyPanel.tsx` | CAREER_ACADEMIC 배지 |
| `components/landing/LiveStatsCounter.tsx` | CAREER_ACADEMIC 참여자 |
| `components/share/ShareButton.tsx` | CAREER_ACADEMIC 타입 |

### 라이브러리
| 파일 | 주요 변경 |
|------|---------|
| `lib/pass-cut.ts` | CAREER_ACADEMIC + 8필드 |
| `lib/pass-cut-auto-release.ts` | CAREER_ACADEMIC 레이블 |
| `lib/mock-data.ts` | CAREER_ACADEMIC + 8필드 |
| `lib/exam-utils.ts` | CAREER_ACADEMIC + 8필드 |
| `lib/correct-rate.ts` | CAREER_ACADEMIC 캐시 무효화 |
| `lib/admin-answer-keys.ts` | parseExamType CAREER_ACADEMIC |
| `app/api/admin/mock-data/route.ts` | careerPerRegion → 3개 별도 필드 |

---

## 6. 아직 미처리/주의사항

### 6-1. 서버 실행 이슈
- `tailwindcss` 모듈 해석이 부모 디렉토리(`합격예측 프로그램/`)를 참조하는 Turbopack 버그 발생
- 회피 방법: Turbopack 비활성화 (`next dev --no-turbo`) 또는 `scripts/dev-single-process.cjs` 사용

### 6-2. 소방학과 CAREER_ACADEMIC 양성 지역 순위 처리
- `toRankGroupKey()`는 성별 포함하지만, `buildPopulationWhere()`는 양성 시 성별 필터 제거
- 이로 인해 **결과 페이지 순위**(scoring.ts 기반)와 **예측 페이지 순위**(prediction.ts 기반)가 다를 수 있음
- TODO: scoring 시점에도 quota를 참조해 rank key를 결정해야 함

### 6-3. 자격증 가산점 UI
- `/exam/input` 페이지에 자격증 가산점 라디오 버튼 (0/2/4/5) 구현 필요
- `Submission.certificateBonus` 필드에 저장하지만 현재 `finalScore` 계산에 포함 안 됨
- 최종 환산 점수 계산 기능은 추후 구현

### 6-4. Vercel 배포
- `fire/` 전용 Vercel 프로젝트 필요 (경찰과 별도)
- GitHub 레포지토리도 별도 생성 권장
- 환경변수 설정 필요 (`.env.local` 참고)

### 6-5. SUPABASE_SERVICE_ROLE_KEY 미설정
- 현재 `.env.local`의 `SUPABASE_SERVICE_ROLE_KEY=""` 빈 값
- 파일 업로드(배너 이미지 등) 기능 사용 시 Supabase Settings → API에서 키 확인 후 입력 필요

### 6-6. 브랜딩 미적용
- 현재 색상이 경찰 블루 계열 유지 중
- 소방 레드 계열(`red-600` 등)로 변경 필요
- `tailwind.config.ts`의 `police-*` 색상 변수명 → `fire-*`로 변경 권장
